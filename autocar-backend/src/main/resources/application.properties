
spring.application.name=autocar-backend

# Banco de Dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/autocar_db?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=j2p220//sp
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# JWT
jwt.secret=autocar_super_secret_key_2024_production_change_me
jwt.expiration=86400000

# CORS (origens permitidas)
cors.allowed-origins=http://localhost:5173,http://localhost:3000
